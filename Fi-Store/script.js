vv = document.querySelectorAll(".imgcontainer");
vv.forEach(function(elem) {
    gsap.to(elem, {
        ease: Expo.easeInOut,
        width: "100%",
        duration: 2,
        stagger: 2
    }) 
});
array.forEach(element => {
    
});